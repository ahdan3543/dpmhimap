
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'i.pravatar.cc', 'images.unsplash.com'],
  },
}

module.exports = nextConfig
